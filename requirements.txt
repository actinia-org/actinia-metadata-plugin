# These requirements are only necessary when actinia-metadata-plugin is used
# as standalone app. In case of installation as actinia-core plugin, the
# requirements are defined in setup.py.

colorlog>=3.1.4
Flask
gunicorn>=19.7.1
jsonmodels>=2.3
pyscaffold
pytest-cov>=2.5.1
requests>=2.20.0
setuptools>=30.3.0
xmltodict>=0.11.0

# pyscaffold needed for s2i build
